# The borg account may accept only the fixed reverse listener used by the
# database backup tunnel. Per-key `restrict` remains the primary default deny.
Match User borg
    AllowTcpForwarding remote
    PermitListen 127.0.0.1:42729
    GatewayPorts no

Match all
