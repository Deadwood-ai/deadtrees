# The borg account's forwarding key may create remote forwards only. The
# private runtime directory and stream-local mask enforce the local UID boundary.
Match User borg
    AllowTcpForwarding remote
    GatewayPorts no
    StreamLocalBindMask 0177
    StreamLocalBindUnlink yes

Match all
